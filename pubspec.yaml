name: notes_app
description: "Notes Management App — Flutter + Firebase Firestore."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  firebase_core: ^3.12.1
  cloud_firestore: ^5.6.5
  firebase_auth: ^5.5.1

  flutter_bloc: ^9.0.0
  equatable: ^2.0.7

  get_it: ^8.0.3
  uuid: ^4.5.1
  intl: ^0.20.1

  shadcn_ui: ^0.41.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
